const studentPoints = 85;
const studentName = "Magdalena";
if (studentPoints >= 75) {
  console.log(`Student ${studentName} has passed the exam.`);
} else {
  console.log(`Student ${studentName} has not passed the exam.`);
}
